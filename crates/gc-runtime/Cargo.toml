[package]
name = "gc-runtime"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
gc-core = { path = "../gc-core" }
gc-platform = { path = "../gc-platform" }
